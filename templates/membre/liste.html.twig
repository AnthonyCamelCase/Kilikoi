
{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center container border border-danger shadow  bg-white rounded">
        <div class="d-flex flex-column align-items-center mb-3 ">

            <!--Créer une liste de lecture à l'utilisateur -->
            <img class=" text-center mb-4" src="{{ asset('image/logo_kilikoi.jpg')}}" alt="" width="144" height="144">
            <h3 class="text-center mb-4">Vous devez créer une liste de lecture :</h3>
            {{ form_start(formListe) }}
                <div class="mb-4 ">{{ form_widget(formListe.nomListe, { 'attr': {'placeholder': "nom de votre liste"}}) }}</div>
                <button class="btn btn-lg btn-danger">Créer votre liste de lecture</button>
            {{ form_end(formListe) }}
        </div>
    </div>
{% endblock %}